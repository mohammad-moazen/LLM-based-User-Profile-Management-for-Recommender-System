[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pure-recommender-reproduction"
version = "0.1.0"
description = "Step-by-step reproduction of PURE for LLM-based recommendation"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
